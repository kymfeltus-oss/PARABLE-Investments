# Picked up by `next build` (e.g. Vercel). Public asset — safe to commit.
# Override in Vercel Project → Environment Variables if this URL changes.
NEXT_PUBLIC_INVESTOR_INTRO_VIDEO_URL=https://u7fkmtuvdvallnwg.public.blob.vercel-storage.com/Investor%20Intro.mp4
