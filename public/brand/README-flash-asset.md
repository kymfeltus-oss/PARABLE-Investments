# Flash intro asset

| File | Role |
|------|------|
| `pitchlock-flash.mp4` | **Production** loop on `/` (must be **2160×3840**, 9∶16) |
| `flash-artwork-master-9x16.svg` | Designer layout guide (safe zone, spacing) |

**Legacy note:** Until replaced, the MP4 may still be ~1080×1215 (~8∶9). That reads cramped on desktop under `object-fit: contain`. Re-export per `docs/flash-artwork-creative-brief.md`.

**App:** `object-fit: contain` only — do not use `cover`.
