import { PagePlaceholder } from "@/components/layout/MarketingShell";

export default function MeetPage() {
  return (
    <PagePlaceholder
      title="Join PitchMeeting"
      description="LiveKit-powered PitchMeeting room. Token minting and pre-join UI will connect here."
      backHref="/dashboard/investor"
    />
  );
}
