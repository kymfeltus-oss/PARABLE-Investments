import { PagePlaceholder } from "@/components/layout/MarketingShell";

export default function BookPage() {
  return (
    <PagePlaceholder
      title="Book PitchMeeting"
      description="Schedule a PitchMeeting with the presenter. Cal.com and confirmation emails will connect here."
      backHref="/dashboard/investor"
    />
  );
}
