import { PresenterAccessPage } from "@/components/presenter/access/PresenterAccessPage";

export const metadata = {
  title: "Presenter Access",
  description: "Sign in or create a Pitch Lock presenter account — demo access to Pitch Builder Studio.",
};

export default function PresenterAccessRoute() {
  return <PresenterAccessPage />;
}
