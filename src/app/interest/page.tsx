import { PagePlaceholder } from "@/components/layout/MarketingShell";

export default function InterestPage() {
  return (
    <PagePlaceholder
      title="Investment Interest"
      description="Signal soft interest, commitment level, or follow-up requests."
      backHref="/pitch"
    />
  );
}
