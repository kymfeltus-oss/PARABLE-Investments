import { PagePlaceholder } from "@/components/layout/MarketingShell";

export default function DocumentsPage() {
  return (
    <PagePlaceholder
      title="Documents Vault"
      description="Secure document library for investors. Tier-gated uploads and watermarks coming next."
      backHref="/pitch"
    />
  );
}
