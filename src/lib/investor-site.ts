/** Canonical site URL for the investor presentation (Vercel / parableinvestments.com). */
export const INVESTOR_SITE_URL = 'https://parableinvestments.com' as const;

/** In-app path — also use with {@link INVESTOR_SITE_URL} in Gamma/iframe buttons (see `proposal-embed` comment). */
export const INVESTOR_FINANCIAL_CALCULATOR_PATH = '/investor/financial-calculator' as const;
