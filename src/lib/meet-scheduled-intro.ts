/** sessionStorage: after user finishes `Live Meeting.mp4`, scheduled `/meet` lobby shows without replay until they visit `/start` again. */
export const LIVE_MEETING_INTRO_SESSION_KEY = 'parable_meet_live_intro_v1';
