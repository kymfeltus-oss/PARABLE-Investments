---
description: After code changes, verify locally; commit and push to GitHub by default.
alwaysApply: true
---

# Deploy / verify after changes

When you finish implementing requested changes in this repo:

1. Run `npm run lint` (and `npm run build` if the change is non-trivial).
2. **Local production (default — no GitHub):** run **`npm run deploy:local`**  
   - Runs `next build` then serves **`http://localhost:3003`** like production.  
   - Stop the server with **Ctrl+C** in the terminal.
3. **Day-to-day dev (hot reload):** `npm run dev` — same port **3003**.

## GitHub (default for this project)

When the user asks to **commit** (or when finishing work that should be saved to the remote), **stage, commit, and push** to the tracked remote (usually **`git push origin main`**). If a different remote/branch is configured, match `git branch -vv` / `git remote -v`.

**Vercel** still only when the user explicitly asks (e.g. “deploy to Vercel”).

Do not commit junk dumps or local-only scratch files unless the user asked.
